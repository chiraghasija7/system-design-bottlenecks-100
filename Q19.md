# Q19. Stock Exchange Order Matching

Match buy and sell orders in real-time. 1M orders/sec. Price-time priority. Must be deterministic — same input always produces same output.

**What's the bottleneck?**

