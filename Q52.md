# Q52. MakeMyTrip Travel Search

User searches "BLR → DEL on date X" — system returns flights from 50+ airlines, prices, layovers, durations. Inventory updates from each airline's API. Low latency, cheap to serve.

**What's the bottleneck?**

