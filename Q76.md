# Q76. Spam / Fraud Detection Pipeline

Score every incoming event (email/login/transaction) for spam/fraud probability. Sub-100ms latency, billions of events/day, models retrained daily.

**What's the bottleneck?**

